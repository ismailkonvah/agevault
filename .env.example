# Environment Variables Template

Copy this file to `.env` and fill in your values.

```env
# Sepolia RPC URL (required for FHEVM)
# Get a free API key from https://www.alchemy.com/
VITE_SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/YOUR_API_KEY

# Optional: Override default Zama gateway URL
# Default: https://gateway.sepolia.zama.ai
# VITE_ZAMA_GATEWAY_URL=https://gateway.sepolia.zama.ai

# Private key for contract deployment (only needed for deployment)
PRIVATE_KEY=your_private_key_here
```

## Vercel Deployment

When deploying to Vercel, add these environment variables in your project settings:

1. Go to your Vercel project dashboard
2. Navigate to **Settings** → **Environment Variables**
3. Add:
   - `VITE_SEPOLIA_RPC_URL` = Your Alchemy Sepolia RPC URL

**Note:** The `PRIVATE_KEY` is only needed for local contract deployment, not for production deployment.
